import { requireTeacher } from "../../../lib/auth/authorization";import { getTeacherTests } from "../../../lib/repositories/teacher-pages.repository";import { TeacherTestsPage } from "../../../components/teacher-pages";
import { TeacherCreateForm } from "../../../components/teacher-create-forms";export const dynamic="force-dynamic";export const revalidate=0;export default async function Page(){const user=await requireTeacher(),data=await getTeacherTests(user.userId);return <><TeacherCreateForm kind="test"/><TeacherTestsPage data={data}/></>}
