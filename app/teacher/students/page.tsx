import { requireTeacher } from "../../../lib/auth/authorization";import { getTeacherStudents } from "../../../lib/repositories/teacher-pages.repository";import { TeacherStudentsPage } from "../../../components/teacher-pages";
export const dynamic="force-dynamic";export const revalidate=0;export default async function Page(){const user=await requireTeacher();return <TeacherStudentsPage data={await getTeacherStudents(user.userId)}/>}
